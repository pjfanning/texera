apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-texera
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Release.Name }}-texera
spec:
  replicas: {{ .Values.texera.numOfPods }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-texera
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-texera
    spec:
      containers:
        - name: texera
          image: {{ .Values.texera.imageName }}
          ports:
            - containerPort: {{ .Values.texera.service.port }}
          env:
            - name: MYSQL_URL
              value: "jdbc:mysql://{{ .Release.Name }}-mysql.{{ .Release.Namespace }}.svc.cluster.local:3306/{{ .Values.mysql.auth.database }}"
            - name: MYSQL_USER
              value: {{ .Values.mysql.auth.username }}
            - name: MYSQL_PASSWORD
              value: {{ .Values.mysql.auth.password }}
{{/*            - name: MONGODB_URL*/}}
{{/*              value: "mongodb://{{ .Release.Name }}-mongodb.{{ .Release.Namespace }}.svc.cluster.local:27017/{{ index .Values.mongodb.auth.databases 0 }}"*/}}
